export enum ChartKey {
  TRANSMISSION = 'transmission',
  FUEL_TYPE = 'fuelType',
  MODEL = 'model',
  YEAR = 'year',
}

export enum ChartOperator {
  LESS_THAN = '<=',
  LESS = '<',
  EQUAL = '===',
  MORE = '>',
  MORE_THAN = '>=',
}
